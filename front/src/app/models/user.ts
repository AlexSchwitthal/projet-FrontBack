export class User {
/*     _id?: number | string;
    login: string;
    password: string;
    nickname: string;
    following: string[]; */

    constructor(public _id: any, public login: string, public password: string, public nickname: string, public following: string[]) {}
}
